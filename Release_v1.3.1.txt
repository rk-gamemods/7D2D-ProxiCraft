[size=4][b]ProxiCraft v1.3.1[/b][/size]

Multiplayer stability and thread safety fixes.

[b]Changes:[/b]
[list]
[*][b]Fixed InvalidOperationException in multiplayer[/b] - When a remote player reconnected, the game's network thread could add/remove entities or load/unload chunks while ProxiCraft was iterating those collections during cache rebuild. All entity list, chunk dictionary, and locked container iterations now use defensive snapshots to prevent concurrent modification crashes.
[*][b]Fixed race condition on open container references[/b] - CurrentOpenContainer, CurrentOpenVehicle, CurrentOpenWorkstation, and CurrentOpenDrone could be set to null by the UI thread between a null-check and subsequent use, causing NullReferenceException during cache rebuild. References are now captured to local variables.
[*][b]Fixed operator precedence bug in VirtualInventoryProvider[/b] - Incorrect evaluation when Config was null caused the mod to attempt storage access instead of falling back to inventory-only mode.
[*][b]Added volatile to cache invalidation flags[/b] - Prevents compiler from serving stale cached values when InvalidateCache() is called from a different context than the cache reader.
[/list]

[b]Installation:[/b]
[list=1]
[*]Download ProxiCraft-1.3.1.zip
[*]Extract the zip into your Mods folder
[*]Launch game with [b]EAC disabled[/b]
[/list]

[b]Multiplayer:[/b] All players need the same version installed.

[b]Bug Reports:[/b] Comment on this mod page with pc_debug.log from your ProxiCraft folder.
